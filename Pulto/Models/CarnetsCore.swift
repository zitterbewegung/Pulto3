//
//  CarnetsCore.swift
//  Pulto3
//
//  Created by Joshua Herman on 9/9/25.
//  Copyright © 2025 Apple. All rights reserved.
//


// CarnetsCore.swift
/*
import Foundation

enum CarnetsCore {
    @discardableResult
    static func startLocalJupyterServer(root: URL, port: Int = 8888) throws -> URL {
        // Set dirs Carnets expects. Adjust to your fork if needed.
        setenv("HOME", root.path, 1)
        setenv("JUPYTER_DATA_DIR", root.appendingPathComponent(".jupyter").path, 1)
        setenv("IPYTHONDIR", root.appendingPathComponent(".ipython").path, 1)

        // TODO: Call into your fork’s true launcher here.
        // e.g. CarnetsLauncher.start(at:root.path, port:port)
        NSLog("[CarnetsCore] (stub) startLocalJupyterServer at \(root.path)")
        return URL(string: "http://127.0.0.1:\(port)")!
    }

    static func stopLocalJupyterServer() {
        // TODO: Call your fork’s stop function if available.
        NSLog("[CarnetsCore] (stub) stopLocalJupyterServer")
    }
}
*/
