import SwiftUI

struct ActiveWindowsSheetWrapper: View {
    var body: some View {
        Text("Active Windows Sheet")
            .frame(width: 400, height: 300)
    }
}

#Preview {
    ActiveWindowsSheetWrapper()
}